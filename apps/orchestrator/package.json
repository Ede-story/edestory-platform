{
  "name": "@edestory/orchestrator",
  "private": true,
  "version": "0.1.0",
  "description": "Backend orchestrator exposing a Design Registry API and AI orchestration endpoints.",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "build": "python -m pip install -r requirements.txt"
  },
  "dependencies": {}
}
